// setupTests.js

// Adds custom matchers for DOM assertions
import '@testing-library/jest-dom';

// Adds accessibility testing matchers (e.g., toHaveNoViolations)
import 'jest-axe/extend-expect';

// You can also configure global test settings or mock APIs here if needed.
